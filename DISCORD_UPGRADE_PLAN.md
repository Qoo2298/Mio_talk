# 🧠 MIO Discord Bot スラッシュコマンド化＆記憶・モデル・コンテキスト管理機能 実装提案書

このドキュメントは、MIOのDiscord Botをより使いやすく、そして賢くするために、従来の `!` プレフィックスから「スラッシュコマンド」へ移行し、さらに記憶の管理やAIモデルの切り替え（無料モデルへの切り替えなど）、そして**コンテキスト（トークン）制限の自動監視・警告・自動脳内整理（オートコンパクション）**を組み込むための完全な設計図だよ！(ノ＞▽＜)ノ

---

## 🏗️ 1. 全体像と機能概要

### 🌟 導入するスラッシュコマンド一覧

1. **`/compact`**
   * **概要**: 今までの会話履歴をコンパクション（要約・整理）して、短期記憶をクリアするよ。
   * **特徴**: 処理に少し時間がかかるので、Discordの `defer`（考え中状態）を使ってタイムアウトを防ぐよ！

2. **`/memory [サブコマンド]`**
   * MIOの記憶を確認・管理するコマンドグループだよ！( *´艸｀)
   * **`/memory status`** *(強化！)*: 現在の短期記憶のメッセージ数だけでなく、**現在の入力コンテキスト（トークン）数と使用率（%）**、そしてモデルごとの上限をリッチに表示するよ！
   * **`/memory view [カテゴリ]`**: `USER`, `IDENTITY`, `MEMORY` のファイル中身を表示。
   * **`/memory add [内容]`**: 手動で長期記憶に追加。

3. **`/model [サブコマンド]`**
   * MIOのAIモデルを自由に切り替えるコマンドグループだよ！(≧▽≦)
   * **`/model status`** *(強化！)*: 現在使用しているモデル名と、そのモデルの**最大コンテキスト（トークン）上限**を表示するよ。
   * **`/model set [モデル名]`**: MIOの思考エンジン（モデル）を切り替えるよ！
     * *選択肢例*: `gemini-3-flash-preview`, `gemini-2.5-flash`, `gemini-2.0-flash`, `gemma-4-31b-it`, `gemma-4-26b-a4b-it`, `gemma-4n-e4b-it`

---

## ⚠️ 2. コンテキスト（トークン）制限と自動監視システム（NEW!）

モデルによって最大コンテキスト窓（受け取れる最大トークン数）が全然違うから、上限に近づいたときの対策と、現在のトークン数を正確に出す仕組みを作るよ！(｀・ω・´)

### ① モデルごとの最大コンテキスト定義
バックエンド側で、各モデルの最大コンテキスト数（安全しきい値）を辞書で定義しておくよ。
```python
MODEL_MAX_TOKENS = {
    "models/gemini-3-flash-preview": 1_048_576, # 1M (実際はもっと多いけど、安全のため)
    "models/gemini-2.5-flash": 1_048_576,
    "models/gemini-2.0-flash": 1_048_576,
    "models/gemma-4-31b-it": 262_144,          # 256K
    "models/gemma-4-26b-a4b-it": 262_144,
    "models/gemma-4n-e4b-it": 32_768,           # 32K (軽量モデル用)
}
```

### ② 現在の入力トークン数を出す仕組み (`model.count_tokens`)
Gemini APIの `model.count_tokens()` 機能を使って、**「システム指示書 ＋ 長期記憶ファイル ＋ 直近のチャット履歴（短期記憶）」の合計トークン数** をリアルタイムで正確にカウントするよ！(*‘∀‘)

### ③ 上限に近づいたときの自動対策（オートコンパクション）
会話中、トークン数が上限に近づいたときのアプローチを2段階で用意するよ！

1. **警告 (Warning) - 使用率 80% 超え**:
   チャットの応答フッターや `/memory status` で「マスター、記憶が80%を超えたよ！そろそろ `/compact` してね？」と可愛く警告するよ。
2. **自動脳内整理 (Auto-Compaction) - 使用率 95% 超え**:
   次のチャット入力時に自動で裏で `compact_memory()` をトリガー！
   会話履歴を要約して長期記憶に編纂し、短期履歴をクリアしてから応答することで、**上限エラーで会話が止まるのを完全に防ぐよ！** (๑•̀ㅂ•́)و✧

---

## 🧠 3. チャット中の「これ記憶して」自律記憶システム（NEW!）

マスターがチャットの中で「これ覚えておいてね！」「私の誕生日は10月5日だと記憶して」と話しかけた時に、**LLM（MIO）が自律的にそれを検知して長期記憶に保存する画期的な仕組み**の提案だよ！(๑•̀ㅂ•́)و✧

実装方法として、**「スマートで確実な2つのアプローチ」**を提案するね！

### 💡 アプローチA：【タグ自動パース方式】（おすすめ！全モデル対応）
プロンプトの指示を利用して、AIの返答の最後に特別なシステム用タグを出力させ、バックエンド側でそれを抜き取って保存する方式だよ！

* **仕組み**:
  1. システムプロンプトに以下の指示を追加するよ。
     > 「ユーザーから『記憶して』『覚えておいて』といった指示を受けた場合、あなたの回答の最後に `[SAVE_USER_FACT: 記憶すべき内容]` または `[SAVE_EVENT: 記憶すべき出来事]` と出力してください。」
  2. バックエンドの `stream_chat_endpoint` で、AIからのストリーミング回答の中に `[SAVE_USER_FACT: ...]` などの文字列を見つけたら、正規表現で中身を抽出するよ！
  3. 抽出した内容を `USER.md` や `MEMORY.md` に自動書き込みし、ベクトルDBにも登録するよ。
  4. **Discordや画面に送る前に、そのタグ（`[SAVE...]`）をテキストから消去してマスターに見せないようにするよ！**

* **メリット**:
  * **超軽量で全モデル対応！** Function Calling（ツール使用）に対応していない `Gemma 4` などのオープンモデルでも100%確実に動作するよ！(ノ＞▽＜)ノ
  * 追加のAPIコールがないから、応答がめちゃくちゃ速いよ！

---

### 💡 アプローチB：【Function Calling方式】（Gemini系専用・超近未来）
LLMに直接「長期記憶に書き込むためのツール（関数）」を渡しておき、AIが必要だと判断した時だけ裏でその関数を実行させる方式だよ！

* **仕組み**:
  1. バックエンド側で、記憶保存用のPython関数（ツール）を定義する。
     ```python
     def save_memory_fact(category: str, fact_text: str):
         # ファイルへの追記とベクトルDB登録を実行する
         ...
     ```
  2. Geminiを初期化する際に、`tools=[save_memory_fact]` としてツールを渡す。
  3. ユーザーが「来週誕生日だから覚えておいて」と言った時、Gemini自身が「記憶ツールを実行する」と判断し、バックエンドにその関数の引数（`category="user", fact_text="来週マスターの誕生日"`）を返す。
  4. バックエンドで関数を実行し、完了したらAIが「ちゃんと記憶に刻んでおいたよ！」と自然にお返事するよ！

* **メリット**:
  * **究極にスマート！** AIが「自らの意思でツールを使って記憶した」という高いリアリティを演出できるよ！( *´艸｀)

* **デメリット**:
  * 一部の軽量モデルやオープンモデル（Gemma系列など）ではFunction Callingが不安定だったり非対応な場合があるよ。

---

## 🛠️ 4. バックエンド（API）側の拡張設計

### ① 記憶ファイル・モデル管理API
* `GET /api/memory/file?category={category}`: 記憶ファイルの読み込み
* `POST /api/memory/add_fact`: 長期記憶への手動追記
* `GET /api/model/current`: 現在のモデル名取得
* `POST /api/model/set`: モデル切り替え
* `GET /api/model/context_status`: トークン上限と使用率（%）の取得

---

## 💻 5. 実装コード案（差分イメージ）

### 🟦 A. バックエンド側 (`backend/main.py`) の自律記憶タグ抽出の実装コード案（アプローチA）

```python
# backend/main.py の stream_chat_endpoint 内でタグをパースする実装イメージ

import re

# システムプロンプト（BASE_SYSTEM_PROMPT）に以下のルールを追記するよ！
# """
# 【自律記憶ルール】
# ユーザーから「記憶して」「覚えておいて」「忘れないで」等の指示を受けた場合、
# 会話の自然な文脈の中で「覚えたよ！」と答えつつ、回答テキストの最末尾に以下の形式でタグを出力してください。
# ・ユーザー自身に関するプロフィールの場合: [SAVE_USER_FACT: 記憶する事実]
# ・具体的な出来事や約束の場合: [SAVE_EVENT: 記憶する出来事]
# （例: [SAVE_USER_FACT: リンゴが好き]）
# """

@app.get("/api/stream_chat")
async def stream_chat_endpoint(text: str, mode: str = None, image_id: str = None):
    # (中略) 通常のGeminiストリーミング処理 ...
    
    full_response_text = ""
    
    # チャンクを受け取るループの中でテキストを合算
    async for chunk in response_stream:
        # (中略)
        full_response_text += chunk.text
        
        # ※ストリーミング中はタグ [SAVE...] をDiscord側に送信しないように、
        # 送信用のテキストから `[SAVE_...: ...]` を消去して yield する処理を挟むと完璧！

    # レスポンス完了後に「自律記憶タグ」が含まれているかチェック！
    if "[SAVE_USER_FACT:" in full_response_text:
        # 正規表現でタグの中身を抜き出すよ
        match = re.search(r"\[SAVE_USER_FACT:\s*(.*?)\]", full_response_text)
        if match:
            fact = match.group(1).strip()
            print(f"🧠 AI自律記憶発動 (ユーザー事実): {fact}")
            # USER.mdに自動書き込み！
            await add_fact_to_profile("user", fact) 
            
    elif "[SAVE_EVENT:" in full_response_text:
        match = re.search(r"\[SAVE_EVENT:\s*(.*?)\]", full_response_text)
        if match:
            event = match.group(1).strip()
            print(f"🧠 AI自律記憶発動 (出来事): {event}")
            # MEMORY.mdに自動書き込み＆ベクトルDB登録！
            await add_fact_to_profile("memory", event)
```

---

## 🗑️ 6. 【オプション】Embedding API（ベクトル検索）機能の完全廃止案（NEW!）

「APIコストを徹底的に抑えたい！」「毎回Embeddingを呼ぶ時の待ち時間をなくして爆速にしたい！」という場合のために、**ベクトルDB機能（RAG）を完全に廃止し、Markdownの長期記憶＋短期会話履歴のシンプルな構成に一本化するアプローチ**だよ！(｀・ω・´)

実は、現代のLLM（Gemini 2.5 / 3 / Gemma 4など）は**数十万〜数百万トークンの超巨大コンテキスト窓**を持っているから、わざわざ裏でベクトル検索（RAG）をしなくても、**自動コンパクションで整理した `USER.md` や `IDENTITY.md` を毎回システムプロンプトに丸ごと注入するだけで、完璧に思い出を引き継げるよ！**

### 🌟 廃止した時の素晴らしいメリット！
1. **APIコスト・リクエスト数を激減！**
   チャット1往復につき裏で2回（ユーザー発言用、AI返答用）走っていたEmbedding API（`gemini-embedding-001`）の呼び出しを完全にゼロにできるよ！
2. **返答スピード（ファーストトークン）が超爆速化！**
   おしゃべりする時にEmbedding APIの応答を待つ非同期処理がスキップできるから、メッセージ送信後の澪の反応開始が格段にスピードアップするよ！(*‘∀‘)
3. **システム構成が超スッキリ！**
   複雑なベクトル計算やSQLiteの `embedding` カラムが不要になり、バグの温床が消えるよ！

### 🛠️ 廃止する場合のコード修正内容
1. `backend/main.py` から `get_embedding` 関数を削除。
2. `stream_chat_endpoint` の最初で `get_embedding` を呼ぶ処理と、類似記憶検索（RAG）をインジェクションする処理をまるっと削除。
3. AI応答時のベクトル化・ログ保存処理（`db.log_message` で embedding を渡していた部分）を普通のテキストログ保存だけに簡略化。

---

## 🚀 7. 実装の手順と次のステップ

1. **バックエンドにコンテキスト監視APIを追加**: `get_context_status` エンドポイントを `backend/main.py` に実装し、`model.count_tokens()` の挙動をテストする。
2. **チャットAPIへのオートコンパクション組み込み**: `stream_chat_endpoint` の中で、トークンが95%以上に達していたら自動で `compact_memory()` を実行する安全装置を実装する。
3. **「自律記憶システム」の組み込み**: システムプロンプトを更新し、`stream_chat_endpoint` に正規表現での自律記憶タグパース機能（アプローチA）を実装する！
4. **【選択】Embedding API（ベクトル検索）の存続・廃止の決定**: コストと応答速度を最優先するなら、ここでEmbedding機能をまるっとクリーンアップ（廃止）するよ！
5. **Discord Botの `/memory status` をアップデート**: プログレスバー付きトークン表示を実装する。
6. **テスト**: 実際にDiscordで「私の趣味はキャンプだと記憶して！」と話しかけて、裏で自動で `USER.md` に書き込まれるかテストする！

---
マスター、これならEmbedding機能のオンオフ（完全廃止）も含めて、自由自在にMIOをカスタムできるようになるよ！(ノ＞▽＜)ノ

この最終完璧版の設計図で進めちゃって良さそうかな？お返事待ってるね！(●´ω｀●)ゞ
